@model ColaboradoresModel
@{
    ViewData["Title"] = "Colaboradores";
}
<h1>@ViewData["Title"]</h1>


<div class="text-center forms">
    
    
    <a type="button" asp-controller="Colaborador" asp-action="CadastrarCol" class="btn btn1">Cadastrar Colaborador</a>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Empresa</th>
                <th>Filial</th>
                <th>PIS</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var colaborador in Model.GetAll())
            {
                <tr>
                    <td>@colaborador.ID.ToString("D5")</td>
                    <td>@colaborador.Nome</td>
                    <td>@colaborador.PegarNomeEmpresa()</td>
                    <td>@colaborador.PegarNomeFilial()</td>
                    <td>@colaborador.PIS</td>
                    <td>
                        <div>
                            
                            <a type="button" asp-controller="Colaborador" asp-route-id="@colaborador.ID" asp-action="Alterar" class="btn btn1">Editar Colaborador</a>
                            <a type="button" asp-controller="Colaborador" asp-route-id="@colaborador.ID" asp-action="Deletar" class="btn btn1">Apagar Colaborador</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>